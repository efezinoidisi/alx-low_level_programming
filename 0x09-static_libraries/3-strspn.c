/**
 * _strspn - length of prefix substring
 * @s: string
 * @accept: substring
 *
 * Return: number of bytes of which is in accept
 */

unsigned int _strspn(char *s, char *accept)
{
	(void)s;
	(void)accept;
	return (0);
}
